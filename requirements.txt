# Enhanced Memory MCP Server Requirements
# Pinned to exact versions for supply-chain safety (2026-02-22)

# Primary implementation (server.py) - uses standard library + extensions

# FastMCP implementation - for better MCP protocol compliance
fastmcp==2.12.3

# Code execution security
RestrictedPython==8.1  # Safe Python code compilation
psutil==5.9.5          # Resource monitoring and limits

# RAG Tier 1 Strategy Dependencies
sentence-transformers==4.1.0  # Re-ranking with cross-encoder
redis==5.2.0                   # Cache layer for re-ranking and adaptive routing
qdrant-client==1.15.1          # Hybrid search with BM25 + Vector
anthropic==0.71.0              # Claude API for contextual prefix generation
aiohttp==3.13.3                # SAFLA remote integration async HTTP client